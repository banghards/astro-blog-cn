---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_AUTHOR, SITE_DESCRIPTION, SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="zh-CN">
	<head>
		<BaseHead title={`关于 | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="about-shell">
				<p class="eyebrow">About</p>
				<h1>这是一个适合长期写作的中文博客起点</h1>
				<p class="lead">
					当前作者名默认写成 <strong>{SITE_AUTHOR}</strong>，邮箱与站点标题也都放在配置文件里，方便你统一替换。
				</p>
				<div class="about-grid">
					<article>
						<h2>你主要会改哪里</h2>
						<ul>
							<li><code>src/consts.ts</code>：博客标题、描述、作者名、邮箱。</li>
							<li><code>src/content/blog/</code>：新增或修改 Markdown 文章。</li>
							<li><code>src/pages/index.astro</code>：首页文案与模块布局。</li>
							<li><code>src/styles/global.css</code>：配色、字体、全局风格。</li>
						</ul>
					</article>
					<article>
						<h2>适合放什么内容</h2>
						<ul>
							<li>AI 工具评测、模型实测、效率工作流。</li>
							<li>开发随笔、独立项目记录、产品观察。</li>
							<li>读书笔记、清单型文章、个人经验整理。</li>
						</ul>
					</article>
				</div>
				<article class="next-step">
					<h2>下一步建议</h2>
					<p>
						先替换站点信息，再把你现有的 Markdown 文稿迁进来。你已经有不少内容草稿，完全可以直接作为首批文章发布。
					</p>
				</article>
			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	.about-shell {
		padding: 1rem 0 2rem;
	}

	.eyebrow {
		margin-bottom: 0.8rem;
		font-size: 0.82rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: rgb(var(--gray));
	}

	.lead {
		max-width: 44rem;
		font-size: 1.08rem;
	}

	.about-grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 1.5rem;
	}

	.about-grid article,
	.next-step {
		padding: 1.4rem;
		border-radius: 1.5rem;
		background: rgba(255, 253, 249, 0.82);
		border: 1px solid rgba(var(--border), 0.8);
		box-shadow: var(--card-shadow);
	}

	.next-step {
		margin-top: 1rem;
	}

	@media (max-width: 720px) {
		.about-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
